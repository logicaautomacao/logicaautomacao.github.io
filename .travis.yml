language: node_js
node_js:
  - "10"
cache:
  directories:
    - "node_modules"
before_deploy:
  - npm run build
  - cp -r build docs
deploy:
  provider: pages
  target-brach: master
  local-dir: docs
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  on:
    branch: master

